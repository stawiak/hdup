test {
    akka.actor.deployment {

      /testdriver/master/workerRouter {

        router = round-robin
        resizer {
              lower-bound = 2
              upper-bound = 20
        }
      }
    }
}